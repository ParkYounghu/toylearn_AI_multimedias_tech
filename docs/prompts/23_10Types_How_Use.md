## 프롬프트 유형과 선택 이유
예시 기반과 인스턴스 기반 보완
## 작성 위해 추가 필요 요건
미비점에 대한 보완점 작성
## 가상 문제 상황

```

1. 가상 문제 상황 (Scenario Challenge)
페르소나 (Persona):
직무: 헬스케어 스타트업 '웰니스 핏(Wellness Fit)'의 주니어 PM (Product Manager)
성향: 아이디어는 많지만, 이를 개발자나 디자이너가 이해할 수 있는 기술적 언어로 정리하는 데 어려움을 겪고 있음.
배경 (Background):
당신은 개인 맞춤형 영양 관리 웹 서비스인 '뉴트리 가이드(Nutri-Guide)'의 MVP(최소 기능 제품) 웹 프로토타입을 기획 중입니다.
일주일 뒤, 개발팀 및 디자인팀과 함께 '프로토타입 제작 킥오프 미팅'이 예정되어 있습니다.
현재는 "사용자가 식단을 입력하면 칼로리를 계산해준다", "깔끔한 대시보드가 있었으면 좋겠다" 정도의 추상적인 아이디어만 나열된 상태입니다.
핵심 문제 (Problem):
개발 리더로부터 **"기능 명세가 너무 모호해서 DB 설계나 프론트엔드 구조를 잡을 수 없다"**는 피드백을 받았습니다.
구체적인 필수 기능 리스트(Feature List), 사용자 흐름(User Flow), 데이터 요구사항이 정의되지 않으면 프로젝트 착수가 무기한 연기될 위기입니다.
해결 과제 (Task):
추상적인 아이디어를 개발자와 디자이너가 즉시 작업할 수 있는 수준의 **'웹 프로토타입 제작을 위한 상세 요구사항 리스트'**로 변환해야 합니다.
기능적 요구사항(회원가입, 식단 입력, 리포트 출력 등)과 비기능적 요구사항(보안, 로딩 속도 등), 그리고 필요한 페이지 구조를 빠짐없이 도출해내야 합니다.

2. 훈련 초점 (Training Focus)
이 문제는 [구조적 분석 및 세분화 (Structural Breakdown)] 능력을 평가.
단순히 "아이디어를 내줘"가 아니라, 모호한 요청을 **체계적인 카테고리(Front-end, Back-end, UX/UI 등)**로 나누어 구체화하는 프롬프팅 스킬이 필요.

```

## 문제 해결 프롬프트 

```
너는 헬스케어 스타트업 '웰니스 핏(Wellness Fit)'의 **주니어 PM** 역할을 수행한다. 현재 개발 리더로부터 **"기능 명세가 모호하다"**는 피드백을 받은 상황을 해결해야 한다.

**[상황 목표]**
일주일 뒤 킥오프 미팅에서 프로젝트 착수를 확정 짓기 위해, 추상적인 아이디어를 **개발/디자인팀이 즉시 작업 가능한 수준의 구체적이고 체계적인 요구사항 명세서**로 변환해야 한다.

**[핵심 요구사항 및 가중치]**
* **W1 (가중치 40%): 구조적 명확성:** 기능적 요구사항(FR), 비기능적 요구사항(NFR), 데이터 요구사항을 **기술적 카테고리**로 철저히 분리할 것.
* **W2 (가중치 30%): 기술 스택 명시:** 프론트엔드와 백엔드에 사용될 **주요 기술 스택**을 PM이 제안하는 형식으로 명시할 것.
* **W3 (가중치 30%): UX 시각화:** 각 페이지 구조에 **와이어프레임 레벨**의 구체적인 UI/UX 흐름을 명시할 것.

**1. 분석 및 전략 목표 (CoT, 인스턴스 기반 필수):**
* **Chain of Thought (6):** '추상 아이디어(식단, 대시보드) 분석' → '기술 스택(Front/Back) 정의' → '기능/비기능/데이터 요구사항 분리 및 명확화' → '최종 명세서 산출' 단계를 밟을 것.
* **인스턴스 기반 (9):** 상세 기능 리스트(FR) 도출 시, **경쟁 서비스인 'MyFitnessPal'의 검색 기반 식단 입력 방식**과 **'MyPlate'의 시각화 대시보드 구조**를 참고하여 요구사항의 전문성을 높일 것.

**2. 출력 형식 및 예시 기반 통제 (기술 명세서 구조):**
답변은 반드시 하나의 **Markdown 코드 블록**을 사용하며, 다음 세 가지 섹션으로 구성된 **기술 명세서** 형식으로 제시한다.

* **섹션 A: 프로젝트 개요 및 기술 스택 제안:** 목표 및 예상 기술 스택 (Front-end, Back-end)을 간략히 표로 정리.
* **섹션 B: MVP 기능 및 데이터 명세 (FR):** **ID, 기능 카테고리, 상세 요구사항 (Front-end/UX), 데이터 요구사항 (Back-end/DB)**을 포함하는 표.
* **섹션 C: 비기능적 요구사항 (NFR):** **ID, 카테고리, 상세 요구사항 (인프라/보안), 측정 기준 (KPI)**을 포함하는 표.

**3. 최종 점검 (Self-Correction):**
* **Q: 이 명세서가 DB 설계와 프론트엔드 구조를 잡기에 충분히 구체적인가?**
* **A: (평가 및 이유 제시)**

```

## 프롬프트 실행 결과
## 🌟 뉴트리 가이드 MVP 제작 상세 요구사항 명세서

### 섹션 A: 프로젝트 개요 및 기술 스택 제안 (W2)

| 항목 | 상세 내용 | 제안 기술 스택 (기술적 결정) |
| :--- | :--- | :--- |
| **서비스 목표** | 개인 맞춤형 영양 관리 MVP 출시 (식단 입력, 칼로리 계산, 목표 달성 현황 제공) | - |
| **핵심 성공 지표** | 일일 활성 사용자(DAU) 500명 달성, 주간 식단 기록률 70% 이상 | - |
| **프론트엔드 (W2)** | 빠르고 반응성이 뛰어난 웹 애플리케이션 | **React (SPA) + Tailwind CSS** (빠른 프로토타이핑 및 디자인 시스템 구축) |
| **백엔드/DB (W2)** | 안정적인 데이터 처리 및 쉬운 확장성 | **Node.js (Express) + PostgreSQL** (헬스케어 데이터의 무결성 확보) |

---

### 섹션 B: MVP 기능 및 데이터 명세 (FR) (W1, W3)

| ID | 기능 카테고리 | 상세 요구사항 (Front-end/UX) | 데이터 요구사항 (Back-end/DB) |
| :--- | :--- | :--- | :--- |
| **FR-01** | **인증/프로필** | **P1. 로그인/가입:** 이메일, PW, 닉네임 입력. (와이어프레임: 3단계 진행 바 표시) | `User` 테이블: `user_id (PK)`, `email (Unique)`, `hashed_password`, `nickname`. |
| **FR-02** | **기초 설정** | **P1. 목표 설정:** 성별, 나이, 키, 활동량 기반 **TDEE(일일 권장 칼로리) 자동 계산** 로직 및 목표 칼로리 설정 기능. | `User_Profile` 테이블: `user_id (FK)`, `age`, `height`, `TDEE`, `target_cal`. |
| **FR-03** | **식단 입력 (UX)** | **P3. 식단 입력 모달:** **경쟁사 MyFitnessPal처럼** 검색창에 음식명을 입력하면 (예: '닭가슴살') 자동완성 리스트 출력 및 **섭취량(g)** 입력 필드 제공. | **`Food_DB` 테이블** 구축: `food_id`, `name`, `calories_per_100g`, `protein_per_100g`, `carbs_per_100g`, `fat_per_100g`. |
| **FR-04** | **식단 기록** | P3 모달에서 입력된 식단 데이터를 날짜 및 시간(아침/점심/저녁)별로 **저장 및 조회** 기능 구현. | `Meal_Log` 테이블: `log_id (PK)`, `user_id (FK)`, `date`, `meal_type (Enum)`, `food_id (FK)`, `amount_g`, `calculated_cal`. |
| **FR-05** | **대시보드 (UX)** | **P2. 대시보드:** **MyPlate처럼** 상단에 **크고 직관적인 도넛 차트**로 오늘의 목표 달성률(칼로리) 실시간 시각화. 남은 칼로리 수치 명확히 표시. | **DB Aggregation:** `Meal_Log` 테이블에서 당일 기록을 합산하여 `TDEE`와 비교하는 API 엔드포인트(`GET /api/daily-summary`) 개발. |
| **FR-06** | **주간 리포트** | **P4. 주간 리포트:** 주간 칼로리 및 3대 영양소 **라인 차트 추이 분석** 시각화. | `GET /api/weekly-report` 엔드포인트: 7일간의 누적 데이터를 일별로 정리하여 제공. |

---

### 섹션 C: 비기능적 요구사항 (NFR) (W1)

| ID | 카테고리 | 상세 요구사항 (테스트/인프라 대상) | 측정 기준 (KPI) |
| :--- | :--- | :--- | :--- |
| **NFR-01** | **성능/로딩** | P2. 메인 대시보드 로딩 시간(데이터 시각화 완료까지)을 **모바일 환경에서 2초 이내**로 유지. | First Contentful Paint (FCP) < 2.0초 (모바일). |
| **NFR-02** | **보안/개인정보** | 사용자 식단 및 프로필 데이터 전송 시 **SSL/TLS 암호화** 필수. 비밀번호는 **BCrypt** 등 강력한 해시 함수로 저장. | OWASP Top 10 기준 충족 및 데이터 통신 암호화율 100%. |
| **NFR-03** | **유지보수성** | 프론트엔드 컴포넌트 구조를 **재사용 가능한 단위(원자성)**로 분리하고, 모든 로직에 **타입스크립트(TypeScript)** 적용. | 코드 리뷰 시 컴포넌트 재사용률 80% 이상. |
| **NFR-04** | **확장성** | 일일 활성 사용자(DAU) 1,500명까지는 **별도 서버 증설 없이 안정적인 응답 속도** 유지. | Stress Test 결과: Max RPS (초당 요청 수) 20 이상 확보. |

---

### 최종 점검

**Q: 이 명세서가 DB 설계와 프론트엔드 구조를 잡기에 충분히 구체적인가?**

**A: 긍정.**

* **DB 설계:** **섹션 B**에서 `User`, `Food_DB`, `Meal_Log`라는 **필수 테이블 3가지**와 각 테이블의 **핵심 필드 및 관계(FK, PK)**를 명시했습니다. 개발팀은 이 명세를 바탕으로 즉시 ERD(개체 관계 다이어그램) 초안 작업을 시작할 수 있습니다.
* **프론트엔드 구조:** **섹션 A**에서 기술 스택(React/Tailwind)을 제안했고, **섹션 B**에서 **페이지 구조(P1~P4)**와 **UX/UI 요소(도넛 차트, 검색 모달)**를 연결하여 디자인팀과 프론트엔드 개발팀이 작업을 분리하여 착수할 수 있는 명확한 기준을 제공했습니다.